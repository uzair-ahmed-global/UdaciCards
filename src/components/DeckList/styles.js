import { StyleSheet } from 'react-native'
export default styles = StyleSheet.create({
    deckCard: {
        flex: 1,
        padding: 18,
        alignItems: 'center',
        justifyContent: 'center'
    },
    subtext: {
        fontSize: 12,
    },
    title:   {
        alignItems: 'center',
        fontSize: 24,
    }
})